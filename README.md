# dotfiles
Exactly What It Says on the Tin

##  other than that
I include a shell script to update the repo after making changes to the dotfiles where they are by default, as well as updating the script itself, I usually keep a directory with all my scripts, so I make the script handle itself, instead of running it from the repository